# <プロジェクト名>

<1〜2行でプロジェクトの目的を書く．何を作るのか，何と連携するのか．>

<!--
このテンプレートの使い方：
- < > で囲んだ箇所を自分のプロジェクトの内容に置き換える
- 使わないセクションは丸ごと削ってよい（短いCLAUDE.mdほど効く）
- 埋めたら冒頭のこのコメントブロックは消す
- 失敗するたびに「ミスの記録」に追記して育てる
-->

## プロジェクト構造

<ディレクトリと役割を列挙する．Claudeに「どこに何があるか」を最初に教える>

- `<dir>/` — <役割．例: 仕様書，UIリファレンス，本体コード>
- `<dir>/` — <役割>
- `<dir>/` — <役割>

## 重要ルール

<触ってよい場所／ダメな場所の境界．スコープ逸脱を防ぐ最重要セクション>

- コードの編集は `<dir>/` 配下でのみ行うこと
- `<dir>/` の外に <生成物の種類> を作らないこと
- 仕様の確認は `<specs等>/` 配下のファイルを読むこと
- <参照すべき外部リファレンスがあれば，それを見てから実装するルール>

## 技術スタック（厳守）

<バージョン・ライブラリ・構成を固定する．勝手な技術選定のブレを防ぐ>

- <言語・バージョン>
- <フレームワーク・主要ライブラリ>
- <アーキテクチャパターン．例: MVVM / レイヤード>
- <ビルドツール・依存管理>
- <ターゲット環境・最小要件>

## ビルドと実行

<推測でビルド設定をいじらせず，決まったコマンドで動かすための具体手順>

- ビルドコマンド: `<command>`
- 実行・起動コマンド: `<command>`
- <実機・外部デバイス・APIが絡む場合は，書く前に準備状況を確認させるルール>

## Git

- コミットメッセージはConventional Commits形式，説明は日本語
- 形式: `type(scope)：日本語の説明`
- 例: `<feat(scope)：機能の説明>`
- 以下の単位でこまめにコミットすること：
  - <意味のある最小単位1>
  - <意味のある最小単位2>
  - バグ修正1件ごと
- 複数機能をまとめて1コミットにしないこと

## 実装ルール

<本のいちばんの肝．「ビルドが通った≠完了」と「推測で書かない」を必ず入れる>

- 1つの機能を実装したら，必ず実際に動作確認できる状態まで持っていく
- テストに実機・外部デバイス・データが必要な場合，コードを書く前にユーザーに準備状況を確認する
- 「ビルドが通った」はテスト完了ではない．実際に動作確認するまで次の機能に進まない
- テストできない環境の場合は，その旨をユーザーに伝えて判断を仰ぐ
- 外部との通信（API・デバイス等）は，コードを書く前に実際にコマンドを実行して挙動を確認すること．推測でコードを書いて余計な手戻りを発生させない

## セッション運用（トークン節約）

- メインセッション（Fable 5）は設計・監査・レビューに専念する
- 実装（コード書き・検証スクリプト作成・定型的なデバッグ）は Agent ツールで Opus/Sonnet のサブエージェントに切り出す
  - 定型的・機械的な実装 → Sonnet、込み入った実装 → Opus を `model` パラメータで指定
  - サブエージェントには理論仕様の該当箇所・対象ファイル・検証方法を明示した自己完結のプロンプトを渡す
- 実装難易度が特に高い箇所（理論式の解釈が絡む中核実装など）はメインセッションで直接実装してよい
- サブエージェントの成果物はメインセッションで必ずレビューし、数値検証の結果を確認してからコミットする

## ミスの記録

<!-- 失敗するたびにここに追記し，同じミスを繰り返さない -->
<!-- 書式： YYYY-MM-DD ／ 症状 ／ 原因 ／ 対策 -->

<例: 2026-MM-DD ／ <症状> ／ <原因> ／ <対策>>
